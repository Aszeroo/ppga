# build artifacts and tool state
node_modules/
.next/
coverage/
test-results/
playwright/
dist/
.vercel/
tsconfig.tsbuildinfo
# environment secrets — NEVER committed
.env*
!/.env.example
!/.gitignore
supabase/.branches/
supabase/.temp/
# ticket #5 source asset — the zip stays out of the repo; the sanctioned .ttf
# ships in `app/fonts/` instead
TA-16-Bit.zip
TA-16-Bit.zip:Zone.Identifier
*.zip:Zone.Identifier